apiVersion: v1
appVersion: "3.2.0"
description: electrs helm chart. The container can be started as middle(guard)- , bridge- , exit-relay or as proxy exposing only the Socks5 Port per default when running.
name: electrs
type: application
version: 3.2.0
