{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "datum-gateway.serviceAccountName" . }}
  labels:
{{ include "datum-gateway.labels" . | nindent 4 }}
{{- end -}}
