apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ckpool.fullname" . }}-test-connection"
  labels:
{{ include "ckpool.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ckpool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
